<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tutoringapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_school"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_school"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        
        <activity 
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".authentication.LoginActivity" />
        <activity android:name=".authentication.RegisterActivity" />
        <activity android:name=".dashboard.DashboardActivity" />
        <activity android:name=".profile.ProfileActivity" />
        <activity android:name=".session.SessionsActivity" />
        <activity android:name=".session.TutorListActivity" />
        <activity android:name=".session.BookSessionActivity" />
        <activity android:name=".payments.PaymentsActivity" />
        <activity android:name=".payments.PaymentHistoryActivity" />
        <activity android:name=".feedback.FeedbackActivity" />
        <activity android:name=".chatbot.ChatbotActivity" />

        <!-- Payment Redirect Handling -->
        <activity 
            android:name=".payments.PaymentResultActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.tutoringapp" />
            </intent-filter>
        </activity>
        
    </application>

</manifest>
